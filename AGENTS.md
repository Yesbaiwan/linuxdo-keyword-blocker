# AGENTS.md

本文件是项目的**开发说明文档**。改代码或改文档前先读它：它指明去哪读什么、必须遵守什么、以及这个项目里已经定型的开发习惯。具体规则在 `docs/` 下，**不要凭记忆改代码**。

## 项目

Linux.do Keyword Blocker —— 单文件 Tampermonkey 用户脚本（`ld-blocker.user.js`），按「类别 / 标签 / 标题」规则自动隐藏或淡化 linux.do 帖子。无构建、无依赖、纯 DOM API，UI 文案简体中文，只面向桌面 Chrome。

## 文档地图

四份文档各有明确边界：**事实只写一处，别处用链接引用**。新增内容按下表找归属，不要往 `AGENTS.md` 里续句子。

| 文档                                       | 讲什么                                                                     | 什么时候读 / 什么内容写这里                         |
| ------------------------------------------ | -------------------------------------------------------------------------- | --------------------------------------------------- |
| [docs/behavior.md](docs/behavior.md)       | 脚本对外表现与匹配语义：启动条件、入口、面板与规则、过滤范围、存储、类别树 | 改任何行为前读；新增行为规则写这里                  |
| [docs/constraints.md](docs/constraints.md) | 改 `ld-blocker.user.js` 必须遵守的写法，每条带「违反了会怎样」             | 动手写代码前读；新增写法约束写这里                  |
| [docs/internals.md](docs/internals.md)     | 机制为什么这么设计——全项目**唯一的「为什么」出处**                         | 改缓存 / 扫描 / 门控 / 容错前读；新增设计理由写这里 |
| [docs/testing.md](docs/testing.md)         | 环境、验证流程、套件、跑法、判读、隐私红线                                 | 跑回归或看失败时读；新增测试与验证内容写这里        |

## 硬性要求

- 改 `ld-blocker.user.js` 前：**必读** [docs/behavior.md](docs/behavior.md) 与 [docs/constraints.md](docs/constraints.md)。
- 改完必须在本站实测并跑回归：见 [docs/testing.md](docs/testing.md)。
- **git 提交 / 推送：用户未明确要求时，严禁任何 git 提交或推送操作。**
- 本地编码与运行规则（评审口径、行数限制、改前先 Read）另见 `.trae/rules/rule.md`。

## 用户偏好与开发习惯

- **反对打补丁式修改**：发现某处设计不好时，该重构就重构、该重写就重写，保持架构清晰；零碎修补只会让代码越来越冗长混乱。
- **不做兼容性回退**：新代码确认正确后，删掉旧路径，不留两份逻辑并存。
- **不做没必要的防御**：只保留有明确出错场景的防御代码；除了系统边界（用户输入、外部接口），内部代码一律信任。
- 先对齐再动手：需求不清楚就问，别按猜测开工；改动后必须验证，包括边缘场景。
- UI 偏好紧凑、信息密集、层级清楚；同一区域的按钮配色一致，添加与编辑的样式必须完全一致。
- 测试要验证真实行为（受控实验 + 真实场景采样），不要「对着答案碰」的断言。
- 文档简洁务实、全文风格统一：该有的内容不省，但不堆砌；行文保持适当的严肃感，避免过度口语化。

## 文档怎么改和维护

- **一处只写一次**。同一件事只在归属它的那个文件里写，别处用链接引用；同一条信息写两处，迟早两边不一致。
- **一条一行**。并列的规则、行为、约束都拆成独立条目；不要用破折号把十几条串成一段。
- **不写会漂移的数字**。断言条数、行数这类会随代码变化的量不写进正文（历史上就把 69 写过而实际成了 72）；必要时引用常量名。
- **该省则省**。改 CSS 时看代码就够的细节（内边距的具体构成、某个符号的视觉居中）不写进文档，写进去反而多一处要同步维护的东西。
- **区分「事实」与「警示」**：事实（机制、格式、语义）只写一处；会伤到人的约束（`data-lkcb-state` 不能用 class、别连着跑回归）在读者必经的路口各给一句短提示 + 链接——读者只读一份文档，不重复等于没写。
- **根 README 只放功能、安装、使用方法**，不写开发说明。README 里若确有涉及开发的句子需要改，同步改本文件，主要内容不要往 README 里搬。
- **改代码时同步文档**：碰了行为就改 `behavior.md`，碰了写法约束就改 `constraints.md`，碰了设计取舍就改 `internals.md`，都不碰就别动文档。
